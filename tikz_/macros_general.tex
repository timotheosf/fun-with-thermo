\def\UnitSize{1.9}
\pgfmathsetseed{\number\time}
\def\L{3}
\def\DeltaL{3.05}
\tikzexternalize[prefix=fig-]
\tikzset{
    external/system call={
        lualatex \tikzexternalcheckshellescape 
        -halt-on-error 
        -interaction=batchmode 
        -output-directory=build
        -jobname "\image" "\texsource"
    }
}

\NewDocumentCommand{\DrawParticle}{ O{DarkBlue!60!black} m O{0.4\baselineskip} O{} }{
    % Sintax:
    %  \DrawParticle[color]{position}[size][name]
    %
    \node[circle, color=#1, fill=#1, inner sep=0pt, minimum size=#3]
        (#4) at (#2) {};
}

\NewDocumentCommand{\DrawSquare}{ O{PrimaryColor} O{PrimaryBackground} m O{1.9} }{
    % Sintax:
    %  \DrawSquare[draw_color][fill_color]{position}[length size]
    %
    \draw[color=#1, fill=#2, thick, shift={(#3)}] 
        (-#4/2, -#4/2) rectangle (#4/2, #4/2);
}

\NewDocumentCommand{\DrawLatticeCell}{ O{PrimaryBackground} m O{1.9} }{
    % Sintax:
    %  \DrawLatticeCell[fill_color]{position}[length size]
    %
    \fill[color=#1,shift={(#2)}] 
        (-#3/2, -#3/2) rectangle (#3/2, #3/2);
}

\NewDocumentCommand{\tikzFig}{
    O{figure}
    m
    O{TikZ figure caption.}
    O{}
}{  
    % Sintax:
    %  \insertTikz[caption setup type (figure)]{tikz macro}[caption][label]
    %
    \begin{center}
        \captionsetup{type=#1}
        #2
        \caption{#3}%
        \label{fig:#4}%
    \end{center}
}

\NewDocumentCommand{\PlotFig}{ O{} m O{ThemeColor}}{
    \begin{tikzpicture}[baseline]
        \PlotAxis[#1]{#2}[#3]
    \end{tikzpicture}%
}

\NewDocumentCommand{\PlotAxis}{ O{} m O{ThemeColor} +O{}}{
    \DrawPlot[#1]{
        \addplot [#3, very thick] { #2 };
        #4
}%
}

\NewDocumentCommand{\DrawPlot}{O{} +m}{
    \begin{tikzpicture}[baseline]
    \begin{axis}[
        grid=major,
        samples=100,
        axis lines=middle,
        enlargelimits=true,
        xlabel style={
        at={(current axis.south)},
        anchor=north,
        yshift=-1.5ex},
        ylabel style={
        yshift=1ex,
        anchor=north west},
        #1
    ]
        #2        
    \end{axis}
    \end{tikzpicture}
}

\NewDocumentCommand{\CenterPlot}{O{} +m}{
    \begin{center}
    \DrawPlot[#1]{#2}
    \end{center}
}